<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.craig.clint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TopLevelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailWesternActivity" />
        <activity android:name=".MainWesternActivity" />
        <activity android:name=".MainDirtyActivity" />
        <activity android:name=".DetailDirtyActivity" />
        <activity android:name=".DetailOtherActivity" />
        <activity android:name=".MainOtherActivity" />
        <activity android:name=".OrderActivity" />
    </application>

</manifest>